@page "/login"
@rendermode InteractiveServer

@using System.Net.Http.Json
@using System.Text.Json
@using Microsoft.JSInterop
@using Navi.ToolsAssets.Admin.Services.Auth

@inject IHttpClientFactory HttpClientFactory
@inject NavigationManager Navigation
@inject WebAuthSessionService AuthSession
@inject IJSRuntime JS

<PageTitle>Ingreso - NAVI Herramientas y AF</PageTitle>

<div class="login-shell">
    <header class="login-topbar login-topbar-balanced">
        <div class="login-brand login-brand-balanced">
            <div class="login-brand-icon login-brand-icon-balanced">N</div>

            <div class="login-brand-copy login-brand-copy-balanced">
                <h1>
                    <span class="brand-navi">NAVI</span>
                    <span class="brand-tools">Herramientas y AF</span>
                </h1>
                <p>Consola de administración</p>
            </div>
        </div>
    </header>

    <main class="login-main">
        <section class="login-card">
            <div class="login-card-icon login-user-card-icon" aria-hidden="true">
                <span class="login-user-head"></span>
                <span class="login-user-body"></span>
            </div>

            <h2>Bienvenido</h2>

            <p class="login-subtitle">
                Ingresa tus credenciales para acceder<br />
                a la consola de administración.
            </p>

            @if (!string.IsNullOrWhiteSpace(errorMessage))
            {
                <div class="login-alert">@errorMessage</div>
            }

            <EditForm Model="loginModel" OnValidSubmit="LoginAsync">
                <div class="login-field">
                    <label>Documento / usuario</label>

                    <div class="login-input-wrap">
                        <span class="login-input-icon" aria-hidden="true">&#128100;</span>

                        <InputText class="login-input"
                                   @bind-Value="loginModel.UserName"
                                   placeholder="Ingresa tu documento o usuario" />
                    </div>
                </div>

                <div class="login-field">
                    <label>Contraseña</label>

                    <div class="login-input-wrap">
                        <span class="login-input-icon" aria-hidden="true">&#128274;</span>

                        <InputText class="login-input"
                                   type="@passwordInputType"
                                   @bind-Value="loginModel.Password"
                                   placeholder="Ingresa tu contraseña" />

                        <button type="button" class="login-eye" @onclick="TogglePassword">
                            @((MarkupString)(showPassword ? "&#128065;" : "&#128065;"))
                        </button>
                    </div>
                </div>

                <button class="login-submit" type="submit" disabled="@isLoading">
                    @(isLoading ? "Ingresando..." : "Ingresar")
                </button>
            </EditForm>
        </section>
    </main>

    <footer class="login-footer">
        Navitrans 40 años <span>•</span> Soluciones que impulsan tu operación
    </footer>

    <div class="login-bg-letter">N</div>
    <div class="login-bg-lines"></div>
    <div class="login-bg-dots"></div>
</div>

@code {
    private const string StorageKey = "navi-auth-session";

    private LoginFormModel loginModel = new();
    private bool isLoading;
    private bool showPassword;
    private string? errorMessage;

    private string passwordInputType => showPassword ? "text" : "password";

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (!firstRender)
        {
            return;
        }

        if (AuthSession.IsAuthenticated)
        {
            Navigation.NavigateTo(NaviPermissionRouteService.GetStartRoute(AuthSession), forceLoad: true);
            return;
        }

        try
        {
            var json = await JS.InvokeAsync<string?>("localStorage.getItem", StorageKey);

            if (!string.IsNullOrWhiteSpace(json))
            {
                var user = JsonSerializer.Deserialize<AuthSessionUser>(
                    json,
                    new JsonSerializerOptions { PropertyNameCaseInsensitive = true });

                if (user is not null && !string.IsNullOrWhiteSpace(user.UserName))
                {
                    AuthSession.Login(user);
                    Navigation.NavigateTo(NaviPermissionRouteService.GetStartRoute(AuthSession), forceLoad: true);
                }
            }
        }
        catch
        {
            await JS.InvokeVoidAsync("localStorage.removeItem", StorageKey);
        }
    }

    private async Task LoginAsync()
    {
        errorMessage = null;

        if (string.IsNullOrWhiteSpace(loginModel.UserName) ||
            string.IsNullOrWhiteSpace(loginModel.Password))
        {
            errorMessage = "Debe ingresar documento/usuario y contraseña.";
            return;
        }

        isLoading = true;

        try
        {
            var api = HttpClientFactory.CreateClient("NaviApi");

            var response = await api.PostAsJsonAsync("api/auth/login", new
            {
                userName = loginModel.UserName,
                password = loginModel.Password
            });

            if (!response.IsSuccessStatusCode)
            {
                var error = await response.Content.ReadAsStringAsync();

                errorMessage = string.IsNullOrWhiteSpace(error)
                    ? "Usuario o contraseña inválidos."
                    : error;

                return;
            }

            var user = await response.Content.ReadFromJsonAsync<AuthSessionUser>();

            if (user is null)
            {
                errorMessage = "No se recibió la información de sesión.";
                return;
            }

            AuthSession.Login(user);

            var json = JsonSerializer.Serialize(user);
            await JS.InvokeVoidAsync("localStorage.setItem", StorageKey, json);

            Navigation.NavigateTo(NaviPermissionRouteService.GetStartRoute(AuthSession), forceLoad: true);
        }
        catch (Exception ex)
        {
            errorMessage = $"No se pudo iniciar sesión: {ex.Message}";
        }
        finally
        {
            isLoading = false;
        }
    }

    private void TogglePassword()
    {
        showPassword = !showPassword;
    }

    private sealed class LoginFormModel
    {
        public string? UserName { get; set; }
        public string? Password { get; set; }
    }
}

<style>
    .login-shell {
        min-height: 100vh;
        position: relative;
        overflow: hidden;
        color: #ffffff;
        display: grid;
        grid-template-rows: auto 1fr auto;
        padding: 2.5rem 3rem;
        background:
            radial-gradient(circle at 48% 44%, rgba(157, 37, 174, .48), transparent 24%),
            radial-gradient(circle at 80% 38%, rgba(118, 0, 136, .42), transparent 26%),
            linear-gradient(135deg, #120016 0%, #24002e 42%, #5b006a 100%);
    }

    .login-topbar {
        position: relative;
        z-index: 5;
        display: flex;
        align-items: flex-start;
        justify-content: space-between;
        gap: 1rem;
    }

    .login-brand {
        display: flex;
        align-items: center;
        gap: 1rem;
    }

    .login-brand-icon,
    .login-card-icon {
        display: grid;
        place-items: center;
        background: linear-gradient(135deg, #8b1fc2, #4b005c);
        color: white;
        font-weight: 950;
        box-shadow: 0 1rem 2rem rgba(0, 0, 0, .26);
    }

    .login-brand-icon {
        width: 62px;
        height: 62px;
        border-radius: .9rem;
        font-size: 1.85rem;
    }

    .login-brand h1 {
        margin: 0;
        color: white;
        font-size: 1.95rem;
        font-weight: 950;
        letter-spacing: -.04rem;
    }

    .login-brand p {
        margin: .2rem 0 0;
        color: rgba(255, 255, 255, .68);
        font-size: 1rem;
        font-weight: 700;
    }

    .login-brand-right {
        display: flex;
        align-items: center;
        gap: 1rem;
        color: rgba(255, 255, 255, .72);
        font-size: 1.05rem;
        padding-top: .4rem;
    }

    .login-divider {
        display: block;
        width: 1px;
        height: 38px;
        background: rgba(255, 255, 255, .45);
    }

    .login-main {
        position: relative;
        z-index: 5;
        display: grid;
        place-items: center;
        padding: 2rem 0 1rem;
    }

    .login-card {
        width: min(460px, 100%);
        background: rgba(255, 255, 255, .98);
        color: #2d1236;
        border: 1px solid rgba(255, 255, 255, .75);
        border-radius: 1.25rem;
        padding: 2rem 2.05rem 1.9rem;
        text-align: center;
        box-shadow:
            0 2rem 4.5rem rgba(0, 0, 0, .34),
            inset 0 1px 0 rgba(255, 255, 255, .95);
        backdrop-filter: blur(14px);
    }

    .login-card-icon {
        width: 68px;
        height: 68px;
        border-radius: .85rem;
        margin: 0 auto 1rem;
        font-size: 2rem;
    }

    .login-card h2 {
        margin: 0;
        color: #3a0046;
        font-size: 1.55rem;
        font-weight: 950;
        letter-spacing: -.02rem;
    }

    .login-subtitle {
        margin: .5rem 0 1.55rem;
        color: #6c6473;
        font-size: .92rem;
        font-weight: 650;
        line-height: 1.45;
    }

    .login-alert {
        text-align: left;
        margin-bottom: .95rem;
        background: #fff1f2;
        color: #9f1239;
        border: 1px solid #fecdd3;
        border-radius: .75rem;
        padding: .75rem .85rem;
        font-weight: 850;
        font-size: .78rem;
        line-height: 1.35;
    }

    .login-field {
        text-align: left;
        margin-bottom: 1rem;
    }

    .login-field label {
        display: block;
        color: #2f1938;
        font-size: .78rem;
        font-weight: 950;
        margin-bottom: .38rem;
    }

    .login-input-wrap {
        display: flex;
        align-items: center;
        gap: .55rem;
        min-height: 44px;
        border: 1px solid #d9dee8;
        background: #fff;
        border-radius: .55rem;
        padding: 0 .75rem;
        transition: border-color .15s ease, box-shadow .15s ease;
    }

    .login-input-wrap:focus-within {
        border-color: #8a1296;
        box-shadow: 0 0 0 .18rem rgba(138, 18, 150, .12);
    }

    .login-input-icon {
        width: 26px;
        min-width: 26px;
        height: 26px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        color: #7a118f;
        font-size: 1rem;
        font-weight: 950;
        line-height: 1;
        opacity: .85;
    }

    .login-input {
        flex: 1;
        border: 0;
        outline: 0;
        min-height: 42px;
        font-size: .86rem;
        color: #24142c;
        background: transparent;
    }

    .login-input::placeholder {
        color: #9aa1ad;
    }

    .login-eye {
        width: 28px;
        min-width: 28px;
        height: 28px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        border: 0;
        background: transparent;
        color: #7a118f;
        font-size: .95rem;
        font-weight: 950;
        cursor: pointer;
        padding: .2rem;
        line-height: 1;
        opacity: .85;
    }

    .login-eye:hover {
        opacity: 1;
    }

    .login-submit {
        width: 100%;
        min-height: 46px;
        margin-top: .35rem;
        border: 0;
        border-radius: .55rem;
        background: linear-gradient(135deg, #8d14a8, #4d005e);
        color: #fff;
        font-size: .9rem;
        font-weight: 950;
        box-shadow: 0 .75rem 1.35rem rgba(99, 0, 113, .25);
        transition: transform .12s ease, box-shadow .12s ease, opacity .12s ease;
    }

    .login-submit:hover {
        transform: translateY(-1px);
        box-shadow: 0 .95rem 1.6rem rgba(99, 0, 113, .32);
    }

    .login-submit:disabled {
        opacity: .72;
        cursor: not-allowed;
        transform: none;
    }

    .login-footer {
        position: relative;
        z-index: 5;
        text-align: center;
        color: rgba(255, 255, 255, .78);
        font-size: .86rem;
        font-weight: 650;
    }

    .login-footer span {
        margin: 0 .65rem;
    }

    .login-bg-letter {
        position: absolute;
        z-index: 1;
        right: -5rem;
        bottom: -13rem;
        font-size: 36rem;
        line-height: 1;
        font-weight: 950;
        color: transparent;
        -webkit-text-stroke: 2px rgba(255, 255, 255, .12);
        text-stroke: 2px rgba(255, 255, 255, .12);
        user-select: none;
        pointer-events: none;
    }

    .login-bg-lines {
        position: absolute;
        z-index: 2;
        right: 7%;
        top: 25%;
        width: 360px;
        height: 420px;
        border: 2px solid rgba(255, 255, 255, .13);
        border-left-color: rgba(255, 255, 255, .24);
        border-radius: 7rem 0 0 7rem;
        transform: skewX(-28deg);
        opacity: .75;
        pointer-events: none;
    }

    .login-bg-dots {
        position: absolute;
        z-index: 2;
        right: 4%;
        top: 15%;
        width: 180px;
        height: 180px;
        opacity: .20;
        background-image: radial-gradient(rgba(255, 255, 255, .85) 1px, transparent 1px);
        background-size: 18px 18px;
        pointer-events: none;
    }

    @@media (max-width: 900px) {
        .login-shell {
            padding: 1.35rem;
        }

        .login-topbar {
            flex-direction: column;
        }

        .login-brand-right {
            display: none;
        }

        .login-brand h1 {
            font-size: 1.45rem;
        }

        .login-brand-icon {
            width: 52px;
            height: 52px;
            font-size: 1.55rem;
        }

        .login-card {
            padding: 1.55rem;
        }

        .login-bg-letter,
        .login-bg-lines,
        .login-bg-dots {
            display: none;
        }
    }

/* ============================================================
   NAVI LOGIN GRADIENT PATCH V2
   Degradado corporativo 70% oscuro + transición morada
   ============================================================ */

.login-shell {
    background:
        radial-gradient(circle at 82% 18%, rgba(133, 0, 155, .30), transparent 28%),
        radial-gradient(circle at 10% 0%, rgba(255, 255, 255, .055), transparent 22%),
        linear-gradient(
            105deg,
            #07070b 0%,
            #0b0b10 34%,
            #120017 55%,
            #23002b 70%,
            #530064 86%,
            #85009b 100%
        ) !important;
}

.login-card {
    background:
        linear-gradient(
            145deg,
            rgba(255,255,255,.98) 0%,
            rgba(255,255,255,.94) 72%,
            rgba(250,244,255,.98) 100%
        ) !important;
    border: 1px solid rgba(255,255,255,.52) !important;
    box-shadow: 0 1.2rem 3rem rgba(0,0,0,.28) !important;
}

.login-topbar {
    background:
        linear-gradient(
            100deg,
            rgba(255,255,255,.075) 0%,
            rgba(255,255,255,.035) 70%,
            rgba(133,0,155,.18) 100%
        ) !important;
    border-bottom: 1px solid rgba(255,255,255,.12) !important;
}

.login-brand-icon,
.login-card-icon {
    background:
        linear-gradient(145deg, #d20a11 0%, #ff3d45 100%) !important;
    box-shadow: 0 .65rem 1.35rem rgba(210,10,17,.25) !important;
}

.login-primary-button,
.login-card button[type="submit"] {
    background:
        linear-gradient(105deg, #07070b 0%, #15001c 58%, #530064 86%, #85009b 100%) !important;
    border: 0 !important;
    color: #ffffff !important;
}

/* ============================================================
   FIN NAVI LOGIN GRADIENT PATCH V2
   ============================================================ */

/* ============================================================
   NAVI LOGIN TITLE CLEAN FINAL PATCH
   Quita recuadro del título, elimina barra Herramientas y AF,
   aumenta título 15%
   ============================================================ */

.login-shell {
    background:
        radial-gradient(circle at 82% 18%, rgba(113, 0, 131, .30), transparent 30%),
        linear-gradient(
            105deg,
            #050507 0%,
            #07070b 22%,
            #090910 48%,
            #100014 70%,
            #2b0035 84%,
            #520063 94%,
            #710083 100%
        ) !important;
}

/* Quitar recuadro/fondo visible del título superior */
.login-topbar {
    background: transparent !important;
    border: 0 !important;
    box-shadow: none !important;
}

/* Quitar barrita y texto "Herramientas y AF" del lado derecho */
.login-brand-right,
.login-divider {
    display: none !important;
}

/* Aumentar título superior 15% */
.login-brand h1 {
    font-size: 1.15em !important;
    transform: scale(1.15) !important;
    transform-origin: left center !important;
    letter-spacing: -.02em !important;
}

/* Mantener consola de administración visible y limpia */
.login-brand p {
    margin-top: .12rem !important;
    color: rgba(255,255,255,.82) !important;
}

/* Tarjeta central limpia */
.login-card {
    background:
        linear-gradient(
            145deg,
            rgba(255,255,255,.98) 0%,
            rgba(255,255,255,.94) 72%,
            rgba(250,244,255,.98) 100%
        ) !important;
    border: 1px solid rgba(255,255,255,.52) !important;
    box-shadow: 0 1.2rem 3rem rgba(0,0,0,.28) !important;
}

/* Botón ingresar con degradado igual */
.login-primary-button,
.login-card button[type="submit"] {
    background:
        linear-gradient(
            105deg,
            #050507 0%,
            #090910 46%,
            #100014 70%,
            #520063 94%,
            #710083 100%
        ) !important;
    border: 0 !important;
    color: #ffffff !important;
}

/* ============================================================
   FIN NAVI LOGIN TITLE CLEAN FINAL PATCH
   ============================================================ */




/* ============================================================
   NAVI LOGIN - CABECERA ARMÓNICA + ICONO USUARIO LIMPIO
   ============================================================ */

.login-topbar-balanced {
    justify-content: center !important;
    align-items: center !important;
    text-align: center !important;
    padding-top: 1.15rem !important;
    padding-bottom: .85rem !important;
    min-height: auto !important;
}

.login-brand-balanced {
    width: 100% !important;
    display: flex !important;
    justify-content: center !important;
    align-items: center !important;
    gap: .72rem !important;
    text-align: center !important;
}

.login-brand-icon-balanced {
    width: 48px !important;
    height: 48px !important;
    min-width: 48px !important;
    border-radius: .85rem !important;
    display: grid !important;
    place-items: center !important;
    background: linear-gradient(145deg, #ff2d35, #d90f22) !important;
    color: #fff !important;
    font-size: 1.28rem !important;
    font-weight: 950 !important;
    line-height: 1 !important;
    box-shadow: 0 .65rem 1.35rem rgba(255, 0, 35, .22) !important;
}

.login-brand-copy-balanced {
    display: grid !important;
    justify-items: start !important;
    text-align: left !important;
}

.login-brand-copy-balanced h1 {
    margin: 0 !important;
    display: flex !important;
    align-items: baseline !important;
    justify-content: center !important;
    gap: .48rem !important;
    flex-wrap: nowrap !important;
    color: #fff !important;
    font-size: clamp(1.65rem, 2.45vw, 2.35rem) !important;
    font-weight: 950 !important;
    line-height: .96 !important;
    letter-spacing: -.045em !important;
    transform: none !important;
    white-space: nowrap !important;
    text-align: left !important;
}

.login-brand-copy-balanced h1 span {
    display: inline-block !important;
    white-space: nowrap !important;
}

.login-brand-copy-balanced h1 .brand-navi {
    letter-spacing: -.035em !important;
}

.login-brand-copy-balanced h1 .brand-tools {
    opacity: .98 !important;
}

.login-brand-copy-balanced p {
    margin: .28rem 0 0 !important;
    color: rgba(255,255,255,.86) !important;
    font-size: .88rem !important;
    font-weight: 850 !important;
    line-height: 1.05 !important;
    letter-spacing: .01em !important;
    text-align: left !important;
}

.login-brand-right,
.login-divider {
    display: none !important;
}

/* Ícono limpio: sin emoji, sin fondo blanco tipo copa */
.login-user-card-icon {
    width: 72px !important;
    height: 72px !important;
    border-radius: 1.25rem !important;
    margin: 0 auto .95rem !important;
    position: relative !important;
    display: grid !important;
    place-items: center !important;
    overflow: hidden !important;
    background: linear-gradient(145deg, rgba(123,13,142,.13), rgba(75,0,92,.06)) !important;
    border: 1px solid rgba(123,13,142,.28) !important;
    box-shadow: 0 .75rem 1.65rem rgba(75,0,92,.12) !important;
    color: transparent !important;
    font-size: 0 !important;
}

.login-user-card-icon::before,
.login-user-card-icon::after {
    content: none !important;
}

.login-user-head {
    position: absolute !important;
    top: 17px !important;
    left: 50% !important;
    width: 20px !important;
    height: 20px !important;
    transform: translateX(-50%) !important;
    border-radius: 999px !important;
    background: linear-gradient(145deg, #8a159f, #4b005c) !important;
    box-shadow: 0 .25rem .65rem rgba(75,0,92,.18) !important;
}

.login-user-body {
    position: absolute !important;
    left: 50% !important;
    bottom: 15px !important;
    width: 38px !important;
    height: 24px !important;
    transform: translateX(-50%) !important;
    border-radius: 999px 999px .7rem .7rem !important;
    background: linear-gradient(145deg, #8a159f, #4b005c) !important;
    box-shadow: 0 .25rem .65rem rgba(75,0,92,.15) !important;
}

@@media (max-width: 760px) {
    .login-brand-balanced {
        gap: .55rem !important;
    }

    .login-brand-icon-balanced {
        width: 42px !important;
        height: 42px !important;
        min-width: 42px !important;
        font-size: 1.08rem !important;
    }

    .login-brand-copy-balanced h1 {
        font-size: 1.48rem !important;
        gap: .34rem !important;
    }

    .login-brand-copy-balanced p {
        font-size: .76rem !important;
    }

    .login-user-card-icon {
        width: 66px !important;
        height: 66px !important;
    }
}

/* ============================================================
   FIN NAVI LOGIN - TITULO CENTRADO + ICONO USUARIO
   ============================================================ */

</style>

